const styles = theme => ({
  root: {
    textAlign: 'center',
    paddingTop: theme.spacing.unit * 20,
  },
  dialog: {
    textAlign: 'center',
    paddingTop: theme.spacing.unit * 20,
  },
});

export default styles;
